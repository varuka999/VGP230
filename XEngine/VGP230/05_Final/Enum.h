#pragma once

enum UnitState
{
    UNIT_STATE_INACTIVE = 0,
    UNIT_STATE_MOVING,
    UNIT_STATE_ATTACKING
};

enum WallState
{
    WALL_STATE_INACTIVE = 0,
    WALL_STATE_INTACT,
    WALL_STATE_DESTROYED,
};